# Frontend source layout

- `app`: App composition, routes, and providers.
- `features`: Domain screens and feature-specific hooks.
- `shared/components`: Reusable UI components.
- `shared/services`: API and realtime clients.
- `shared/config`: Runtime configuration constants.
- `pages`, `hooks`, `components`, `context`, `services`, `utils`: Compatibility wrappers for older imports.
